# Codex Continuity Context

Last updated: 2026-06-11

This file is intentionally hidden so a future AI/coding assistant can read it and understand the project history without needing the full chat transcript.

## Project

Project path:

```text
/Users/warren/Projects/Snake-Roguelike
```

Project goal:

Make a browser-based Snake roguelike. The player controls a snake, eats apples, gains score, grows, chooses upgrades, and eventually fights an enemy snake.

The user prefers learning step by step. In several requests, the user explicitly asked Codex not to directly edit the game code and instead to show what code to write and explain why. For this specific file, the user asked Codex to create a hidden backup/context file.

## Current Files

Main files:

```text
game/index.html
game/style.css
game/script.js
game/gamestate.js
```

`game/gamestate.js` exists but is currently not loaded by `index.html`. The active game code is in `game/script.js`.

Current `index.html` loads:

```html
<link rel="stylesheet" href="style.css">
<script src="script.js"></script>
```

Current canvas size:

```html
<canvas id="game" width="1920" height="1440"></canvas>
```

Current HUD has:

```html
Score: <span id="score">0</span>
High Score: <span id="high-score">0</span>
```

## Current Implemented Game Features

Current active implementation in `game/script.js` includes:

- Player snake controlled with WASD or arrow keys.
- Canvas rendering.
- Player snake head is blue.
- Player snake body is green.
- Three apples exist at a time.
- Apples are red.
- Eating apples increases score and apple count.
- Eating apples makes the player snake grow.
- Every 10 apples eaten triggers a `prompt` upgrade menu.
- Current upgrade choices:
  - `1. Speed Up`
  - `2. Double Score`
  - `3. Grow More`
- High score uses `localStorage` key `snakeHighScore`.
- Enemy snake variables exist.
- Enemy snake spawns after the second player upgrade.
- Enemy snake starts in a random corner.
- Enemy snake head is yellow.
- Enemy snake body is purple.
- Enemy gets longer and faster after later player upgrades.
- Enemy currently moves directly toward the player's head with simple distance logic.

## Known Current Code Details And Issues

Important current variable names:

```js
let snake;
let apples;
let score;
let highscore;
let applesEaten;
let scoreMultiplier;
let growthAmount;
let gameSpeed;
let gameInterval;
let upgradesChosen;
let enemySnake;
let enemyActive;
let enemyMoveCounter;
let enemyMoveDelay;
let enemyStartingLength;
```

Current scoring helper:

```js
function addScore(points) {
    score += points * scoreMultiplier;
    scoreElement.textContent = score;

    if (score > highscore) {
        highscore = score;
        highScoreElement.textContent = highscore;
        localStorage.setItem("snakeHighScore", highscore);
    }
}
```

Potential scoring issue:

The current apple code calls:

```js
addScore(10 * scoreMultiplier);
```

Since `addScore` already multiplies by `scoreMultiplier`, this double-counts the multiplier. It should likely become:

```js
addScore(10);
```

The current enemy collision reward calls:

```js
addScore(100 * scoreMultiplier);
```

If the desired reward is a default 100 affected by multiplier, use:

```js
addScore(100);
```

If the desired reward is exactly 100 no matter what, create a separate function like:

```js
function addDefaultScore(points) {
    score += points;
    scoreElement.textContent = score;

    if (score > highscore) {
        highscore = score;
        highScoreElement.textContent = highscore;
        localStorage.setItem("snakeHighScore", highscore);
    }
}
```

## Conversation History Summary

The user first asked for a base Snake game:

- WASD and arrow-key controls.
- Apples appear on the map.
- Apples reappear when eaten.
- Snake grows a little when eating apples.
- Score increases by 10.
- User wanted step-by-step instructions.

Codex explained:

- Use `index.html`, `style.css`, and `script.js`.
- Use a canvas.
- Store the snake as an array of `{ x, y }` body parts.
- Store the apple position.
- Use `keydown` to change direction.
- Use a game loop with `setInterval`.
- Draw the snake and apple on canvas.

The user then had a blank white screen.

Codex diagnosed:

- `index.html` needed `<link rel="stylesheet" href="style.css">`.
- `index.html` was loading `game.js`, but the real file was `script.js`.
- `script.js` had a missing `||` in the wall/self collision condition.
- `setInterval(gameloop, 120)` needed to be `setInterval(gameLoop, 120)`.

The user then saw snake and apple but could not move.

Codex diagnosed:

- `gameLoop` updated the snake but did not redraw every tick.
- Needed to call `draw();` inside `gameLoop`.
- Also reminded user to click/focus the browser page rather than the editor.

The user then asked for:

- Three apples on screen at once.
- Bigger game area.
- Every 10 apples eaten, choose between three upgrades.

Codex showed steps to:

- Make canvas larger.
- Use `tileCountX` and `tileCountY`.
- Replace `apple` with `apples`.
- Add `applesEaten`, `scoreMultiplier`, `growthAmount`, `gameSpeed`, and `gameInterval`.
- Replace single-apple eating/drawing with loop over `apples`.
- Add `createApple()`.
- Add `chooseUpgrade()` with `Speed Up`, `Double Score`, and `Grow More`.

The user then asked for:

- High scores shown on screen.
- Player snake head colored blue.
- Enemy snake spawns after 2 player upgrades.
- Enemy snake spawns in a random corner.
- Enemy snake is purple with a yellow head.
- Enemy grows longer and faster every player upgrade.
- Enemy pathfinds to player's head.
- If enemy hits player's body, enemy respawns and player gains 100 default score.

Codex showed steps to:

- Add `High Score` to HUD.
- Add `highScoreElement`.
- Use `localStorage`.
- Add `addScore`.
- Draw player head blue and body green.
- Add enemy variables.
- Increment `upgradesChosen` in `chooseUpgrade`.
- Spawn enemy when `upgradesChosen === 2`.
- Upgrade enemy when `upgradesChosen > 2`.
- Add `spawnEnemySnake()`.
- Add `upgradeEnemySnake()`.
- Add `updateEnemySnake()`.
- Add `enemyHitsPlayer()`.
- Draw enemy.
- Reset enemy state in `resetGame`.

The user reported enemy did not spawn.

Codex diagnosed:

- `chooseUpgrade()` was incrementing `upgradesChosen` and only calling `upgradeEnemySnake()` when `upgradesChosen > 2`.
- It never called `spawnEnemySnake()`.

Codex instructed replacing:

```js
upgradesChosen++;

if (upgradesChosen > 2) {
    upgradeEnemySnake();
}
```

with:

```js
upgradesChosen++;

if (upgradesChosen === 2) {
    spawnEnemySnake();
} else if (upgradesChosen > 2) {
    upgradeEnemySnake();
}
```

This fix is now present in the current `script.js`.

## Latest Requested Feature Set

The user most recently asked for:

- Enemy snake should be more intelligent.
- Enemy should pathfind to the player's head.
- Enemy should try to avoid contact with the player's body.
- If enemy accidentally eats an apple, enemy should grow.
- Add an invincibility upgrade.
- Invincibility is activated by pressing `V`.
- Invincibility lasts 5 seconds initially.
- Invincibility has a 1 minute cooldown initially.
- Each invincibility upgrade increases both duration and cooldown by 1 second.
- While invincible:
  - Player can go through their own body.
  - Player kills enemy snake no matter where contact happens.
  - Player can touch wall without dying.
  - Player head should glide across/clamp to the wall to avoid body collision.

Codex provided step-by-step instructions but has not directly edited the game files for this feature yet.

Recommended implementation plan from Codex:

1. Add invincibility HUD text to `index.html`:

```html
Invincibility: <span id="invincibility">Locked</span>
```

2. Add this near top of `script.js`:

```js
const invincibilityElement = document.getElementById("invincibility");
```

3. Add invincibility variables:

```js
let invincibilityLevel = 0;
let invincible = false;
let invincibilityReady = true;
let invincibilityDuration = 5000;
let invincibilityCooldown = 60000;
let invincibilityEndTimer;
let invincibilityCooldownTimer;
```

4. In `changeDirection(event)`, after:

```js
const key = event.key.toLowerCase();
```

add:

```js
if (key === "v") {
    activateInvincibility();
    return;
}
```

5. Add functions:

```js
function activateInvincibility() {
    if (invincibilityLevel === 0 || !invincibilityReady || invincible) {
        return;
    }

    invincible = true;
    invincibilityReady = false;
    updateInvincibilityHud();

    clearTimeout(invincibilityEndTimer);
    clearTimeout(invincibilityCooldownTimer);

    invincibilityEndTimer = setTimeout(function () {
        invincible = false;
        updateInvincibilityHud();
    }, invincibilityDuration);

    invincibilityCooldownTimer = setTimeout(function () {
        invincibilityReady = true;
        updateInvincibilityHud();
    }, invincibilityCooldown);
}

function updateInvincibilityHud() {
    if (invincibilityLevel === 0) {
        invincibilityElement.textContent = "Locked";
    } else if (invincible) {
        invincibilityElement.textContent = "Active";
    } else if (invincibilityReady) {
        invincibilityElement.textContent = "Ready";
    } else {
        invincibilityElement.textContent = "Cooldown";
    }
}
```

6. Add option 4 to `chooseUpgrade()` prompt:

```js
const choice = prompt(
    `Choose an upgrade:\n1. Speed Up\n2. Double Score\n3. Grow More\n4. Invincibility`
);
```

7. Add upgrade handling:

```js
if (choice === "4") {
    invincibilityLevel++;
    invincibilityDuration += 1000;
    invincibilityCooldown += 1000;
    invincibilityReady = true;
    updateInvincibilityHud();
}
```

8. Replace player wall/self death check with invincibility-aware logic:

```js
if (invincible) {
    newHead.x = Math.max(0, Math.min(tileCountX - 1, newHead.x));
    newHead.y = Math.max(0, Math.min(tileCountY - 1, newHead.y));
} else if (
    newHead.x < 0 || 
    newHead.x >= tileCountX ||
    newHead.y < 0 || 
    newHead.y >= tileCountY ||
    snakeHitsItself(newHead)
) {
    resetGame();
    return;
}
```

9. Replace `updateEnemySnake()` with smarter scoring-based movement:

```js
function updateEnemySnake() {
    if (!enemyActive) {
        return;
    }

    enemyMoveCounter++;

    if (enemyMoveCounter < enemyMoveDelay) {
        return;
    }

    enemyMoveCounter = 0;

    const enemyHead = enemySnake[0];
    const playerHead = snake[0];

    const moves = [
        { x: 1, y: 0 },
        { x: -1, y: 0 },
        { x: 0, y: 1 },
        { x: 0, y: -1 }
    ];

    let bestMove = moves[0];
    let bestScore = Infinity;

    for (const move of moves) {
        const testHead = {
            x: enemyHead.x + move.x,
            y: enemyHead.y + move.y
        };

        let moveScore = getDistance(testHead, playerHead);

        if (testHead.x < 0 || testHead.x >= tileCountX || testHead.y < 0 || testHead.y >= tileCountY) {
            moveScore += 10000;
        }

        if (enemyWouldHitItself(testHead)) {
            moveScore += 5000;
        }

        if (enemyWouldHitPlayerBody(testHead)) {
            moveScore += 3000;
        }

        if (moveScore < bestScore) {
            bestScore = moveScore;
            bestMove = move;
        }
    }

    const newEnemyHead = {
        x: enemyHead.x + bestMove.x,
        y: enemyHead.y + bestMove.y
    };

    enemySnake.unshift(newEnemyHead);

    const appleIndex = apples.findIndex(function (apple) {
        return apple.x === newEnemyHead.x && apple.y === newEnemyHead.y;
    });

    if (appleIndex !== -1) {
        apples[appleIndex] = createApple();
    } else {
        enemySnake.pop();
    }

    handleEnemyPlayerCollision();
}
```

10. Add helper functions:

```js
function getDistance(a, b) {
    return Math.abs(a.x - b.x) + Math.abs(a.y - b.y);
}

function enemyWouldHitItself(testHead) {
    for (const part of enemySnake) {
        if (testHead.x === part.x && testHead.y === part.y) {
            return true;
        }
    }

    return false;
}

function enemyWouldHitPlayerBody(testHead) {
    for (let i = 1; i < snake.length; i++) {
        if (testHead.x === snake[i].x && testHead.y === snake[i].y) {
            return true;
        }
    }

    return false;
}
```

11. Replace current `enemyHitsPlayer()` logic with:

```js
function handleEnemyPlayerCollision() {
    const enemyHead = enemySnake[0];
    const playerHead = snake[0];

    if (enemyHead.x === playerHead.x && enemyHead.y === playerHead.y) {
        if (invincible) {
            addScore(100);
            spawnEnemySnake();
        } else {
            resetGame();
        }

        return;
    }

    for (let i = 1; i < snake.length; i++) {
        if (enemyHead.x === snake[i].x && enemyHead.y === snake[i].y) {
            addScore(100);
            spawnEnemySnake();
            return;
        }
    }

    if (invincible) {
        for (const enemyPart of enemySnake) {
            if (playerHead.x === enemyPart.x && playerHead.y === enemyPart.y) {
                addScore(100);
                spawnEnemySnake();
                return;
            }
        }
    }
}
```

12. Add enemy avoidance to `createApple()` before returning `newApple`:

```js
for (const enemyPart of enemySnake) {
    if (enemyPart.x === newApple.x && enemyPart.y === newApple.y) {
        return createApple();
    }
}
```

13. Reset invincibility in `resetGame()`:

```js
invincibilityLevel = 0;
invincible = false;
invincibilityReady = true;
invincibilityDuration = 5000;
invincibilityCooldown = 60000;

clearTimeout(invincibilityEndTimer);
clearTimeout(invincibilityCooldownTimer);

updateInvincibilityHud();
```

14. Initialize invincibility HUD before first `draw()`:

```js
updateInvincibilityHud();
```

## Latest Feature Additions (2026-06-19)

The following features were provided as exact code to add. They extend the upgrade system (now 8 options), add arena progression every 15 apples, and introduce board obstacles. All edits target `game/script.js` and `game/index.html`.

### Summary of Changes

| Change | File | Location |
|---|---|---|
| HUD elements | `index.html` | Inside `<div id="hud">` |
| DOM refs + variables | `script.js` | Lines 8-9 area and lines 36-42 area |
| Change upgrade interval 10→15 | `script.js` | Line 131 (`applesEaten % 10` → `% 15`) |
| Time slow `B` key | `script.js` | `changeDirection()`, after V-invincibility block |
| Replace `chooseUpgrade()` | `script.js` | Lines 229-269 (full replacement) |
| Replace death-check with shield | `script.js` | Lines 103-115 |
| Add wall/hazard collision checks | `script.js` | After death-check, before `snake.unshift` |
| Magnet pull | `script.js` | After `direction = nextDirection` |
| Fix `addScore` for piercing | `script.js` | Line 125 |
| Replace `draw()` entirely | `script.js` | Lines 153-198 |
| New functions | `script.js` | Before `resetGame()` (~line 477) |
| Enemy avoids walls/hazards | `script.js` | Inside `updateEnemySnake()` move-scoring loop |
| Time-slow on enemy | `script.js` | Top of `updateEnemySnake()` delay check |
| Update `resetGame()` | `script.js` | After invincibility resets |
| Apple avoids walls/hazards | `script.js` | Inside `createApple()` before return |

### New HUD Elements (`index.html`)

Placed inside `<div id="hud">` (after the Invincibility span):

```html
    Shield: <span id="shield">0</span>
    Time Slow: <span id="time-slow">Locked</span>
    Level: <span id="level">1</span>
```

- **Shield** — shows current shield charges (number).
- **Time Slow** — shows "Locked" / "Ready" / "Active" / "Cooldown" status.
- **Level** — shows current arena level number.

### New DOM References (`script.js`)

Placed after the existing `invincibilityElement` line (line 8):

```js
const shieldElement = document.getElementById("shield");
const timeSlowElement = document.getElementById("time-slow");
const levelElement = document.getElementById("level");
```

### New Variables (`script.js`)

Placed after the invincibility variable block (after line 42):

```js
let shield = 0;                          // number of shield charges
let piercingLevel = 0;                   // bonus score per apple: +5 per level
let timeSlowActive = false;              // is time slow currently active
let timeSlowReady = true;                // is time slow off cooldown
let timeSlowDuration = 10000;            // 10 seconds active
let timeSlowCooldown = 60000;            // 1 minute cooldown
let timeSlowEndTimer;                    // setTimeout handle for duration
let timeSlowCooldownTimer;               // setTimeout handle for cooldown
let magnetLevel = 0;                     // pull range: level * 4 tiles
let level = 1;                           // current arena level
let walls = [];                          // array of {x, y} wall positions
let hazards = [];                        // array of {x, y} hazard positions
let shieldFlashTimer = null;             // setTimeout handle for shield invuln
let shieldInvulnerable = false;          // brief invulnerability after shield proc

const levelColors = [                    // background colors per level
    "#1c232b", "#1a2830", "#1c2e30", "#1e2e3a", "#202e44",
    "#262c4c", "#2c2a50", "#342850", "#3c264c", "#442444"
];
```

### Change Upgrade Interval (10 → 15)

In `gameLoop()`, change:

```js
if (applesEaten % 10 === 0) {
```
to:
```js
if (applesEaten % 15 === 0) {
```

This means upgrades and level progression now happen every 15 apples instead of every 10.

### Time Slow Activation Key (`changeDirection`)

Placed after the invincibility `V` key block:

```js
if (key === "b") {
    activateTimeSlow();
    return;
}
```

Pressing **B** activates time slow. Cannot activate while on cooldown or already active.

### Replacement `chooseUpgrade()` Function

Full replacement of the old `chooseUpgrade()`. Now offers 8 choices via `prompt`:

| Key | Upgrade | Effect |
|---|---|---|
| 1 | Speed Up | Increase game speed (decrease interval by 20ms, min 50) |
| 2 | Double Score | +1 to score multiplier |
| 3 | Grow More | +1 to growth amount (extra segments per apple) |
| 4 | Invincibility | +1 level, +1s duration, +1s cooldown |
| 5 | Shield | +1 shield charge (absorbs one death) |
| 6 | Piercing | +5 extra score per apple per piercing level (currently) |
| 7 | Time Slow | Unlocks/grants B-key time slow ability |
| 8 | Magnet | Pulls apples within range toward player head |

#### Planned Upgrade Scaling (on repeated picks)

These are NOT yet implemented. Documented here for future coding.

| Upgrade | Current Behavior (first pick) | Planned Scaling (each subsequent pick) |
|---|---|---|
| **5-Shield** | +1 charge, 500ms invulnerability on use | +1 charge, +200ms invulnerability duration per level (e.g. level 2 = 700ms, level 3 = 900ms) |
| **6-Piercing** | +5 flat score per apple | Change to +1 flat score per piercing level instead of +5 (nerfs first pick, but makes repeated picks stack linearly) — alternatively keep +5 and let repeated picks add +1 each (so first pick = +5, second = +6, third = +7, etc.) |
| **7-Time Slow** | Unlocks ability (10s duration, 60s cooldown) | +1s to duration and +1s to cooldown per level (same pattern as invincibility) |
| **8-Magnet** | Pull radius = level * 4 tiles | Pull radius increases by +2 tiles per level instead of ×4 multiplier (so level 2 = 6 tiles, level 3 = 8 tiles, etc.) |

Code changes required to implement scaling:
- **Shield**: In `chooseUpgrade()`, add `shieldInvulnDuration = 500 + (shield * 200);` and pass this to `tryUseShield()`, replacing the hardcoded 500ms.
- **Piercing**: Change `addScore(10 + piercingLevel * 5);` to use a different formula, e.g. `addScore(10 + piercingLevel * 1 + (piercingLevel > 0 ? 4 : 0));` for the +5 first then +1 approach.
- **Time Slow**: Already has duration/cooldown variables. In `chooseUpgrade()`, add `timeSlowDuration += 1000; timeSlowCooldown += 1000;` for choice 7.
- **Magnet**: In `chooseUpgrade()`, add `magnetRange = 4 + (magnetLevel - 1) * 2;` where `magnetLevel` is already incremented. Change `applyMagnet()` to use `magnetRange` instead of `magnetLevel * 4`.

After the choice, the function also:
- Calls `upgradesChosen++` and handles enemy spawn/upgrade (unchanged logic)
- Increments `level`, updates the HUD
- Resets the game interval with slightly faster base speed (`120 - level * 5`, min 50)
- Calls `generateLevel()` to create new walls and hazards

### Shield-Aware Death Check

The old wall/self collision block is replaced. Flow:

1. If `invincible` — clamp to walls (unchanged).
2. If hitting wall or self — try `tryUseShield()`. If shield succeeds, clamp and continue. Otherwise `resetGame()`.
3. Wall collision check — iterate `walls[]`. If head matches a wall, try shield or reset.
4. Hazard collision check — iterate `hazards[]`. If head matches a hazard, try shield or reset.

`tryUseShield()` function (new):
- If `shieldInvulnerable` is true, return true (already protected for 500ms).
- If `shield > 0`, decrement shield, set 500ms invulnerability, return true.
- Otherwise return false.

### Magnet Pull

Added right after `direction = nextDirection;` in `gameLoop()`:

```js
if (magnetLevel > 0) {
    applyMagnet();
}
```

`applyMagnet()` function:
- Gets the player head.
- For each apple within `magnetLevel * 4` tiles (Manhattan distance), moves the apple 1 tile closer to the player head each game tick.
- Apples cannot move through walls/hazards (they move tile-by-tile same as entities).

### Piercing Score Change

In the apple-eating block, change:

```js
addScore(10 * scoreMultiplier);
```
to:
```js
addScore(10 + piercingLevel * 5);
```

Each piercing level adds a flat +5 per apple, on top of the multiplier.

### Replacement `draw()` Function

Full replacement. Now draws in order:
1. **Background** — fills canvas with `levelColors[(level - 1) % 10]`.
2. **Walls** — dark gray blocks (`#4a5568`) with darker inner fill (`#2d3748`).
3. **Hazards** — red blocks (`#dc2626`) with darker red inner (`#b91c1c`).
4. **Player snake** — same as before (blue head, green body).
5. **Apples** — same as before (red).
6. **Enemy snake** — same as before (yellow head, purple body).

### New Functions (placed before `resetGame()`)

1. **`tryUseShield()`** — Consumes 1 shield charge on death. Grants 500ms invulnerability (flashes — though no visual flash, just the flag). If already invulnerable from a recent shield use, returns true without consuming another charge.

2. **`activateTimeSlow()`** — Checks ready/active state, sets `timeSlowActive = true`, updates HUD, starts duration timer (10s) and cooldown timer (1min). After duration, deactivates. After cooldown, re-enables.

3. **`applyMagnet()`** — For each apple within `magnetLevel * 4` Manhattan distance, moves it 1 tile closer per tick (directional, not diagonal).

4. **`generateLevel()`** — Called on each level-up. Calculates `wallCount = min(5 + level * 2, 25)` and `hazardCount = min(2 + level, 10)`. Builds a set of occupied positions (snake, apples, enemy). Randomly places walls and hazards on unoccupied tiles (up to 100 attempts each). Walls and hazards cannot overlap the player head or existing entities.

### Enemy AI Updates (`updateEnemySnake()`)

**Wall/Hazard avoidance** — added inside each move's scoring loop:

```js
for (const wall of walls) {
    if (testHead.x === wall.x && testHead.y === wall.y) {
        moveScore += 10000;
        break;
    }
}

for (const hazard of hazards) {
    if (testHead.x === hazard.x && testHead.y === hazard.y) {
        moveScore += 8000;
        break;
    }
}
```

This heavily penalizes moves that would hit walls/obstacles (higher penalty than hitting itself or player body), so the enemy pathfinds around them.

**Time slow** — the enemy delay check becomes:

```js
let effectiveDelay = enemyMoveDelay;
if (timeSlowActive) {
    effectiveDelay = enemyMoveDelay * 2;
}

if (enemyMoveCounter < effectiveDelay) {
    return;
}
```

When time slow is active, the enemy moves at half speed (twice the delay between moves).

### `resetGame()` Updates

Added after the invincibility reset block:

```js
shield = 0;
piercingLevel = 0;
timeSlowActive = false;
timeSlowReady = true;
magnetLevel = 0;
level = 1;
walls = [];
hazards = [];
shieldInvulnerable = false;
clearTimeout(timeSlowEndTimer);
clearTimeout(timeSlowCooldownTimer);
shieldElement.textContent = "0";
timeSlowElement.textContent = "Locked";
levelElement.textContent = "1";
```

### `createApple()` Updates

Added before the final `return newApple;`:

```js
for (const wall of walls) {
    if (wall.x === newApple.x && wall.y === newApple.y) {
        return createApple();
    }
}

for (const hazard of hazards) {
    if (hazard.x === newApple.x && hazard.y === newApple.y) {
        return createApple();
    }
}
```

Prevents apples from spawning inside walls or hazards.

### gameState.js (unused)

The file `game/gamestate.js` exists but is not loaded by `index.html`. It contains old, pre-enemy, pre-upgrade game logic. Consider either deleting it or loading it as a module in the future. It is currently not maintained.

## Development Style Preference

The user often asks to be shown what code to write rather than having Codex directly edit the code. Respect that when requested. If the user later asks Codex to implement it directly, make scoped edits with `apply_patch`.

